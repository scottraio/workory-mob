link_color 	= #369
text_color 	= #444
neue_font 	= "Helvetica Neue Light", "HelveticaNeue-Light", Helvetica, Arial, sans-serif
helv_font 	= Helvetica, Arial, sans-serif
header_blue = #4484DA
error_red 	= #A77377

// Rounded Corners
borderRadius(radius)
	border-radius radius radius radius radius // Opera 105, IE 9, Saf5, Chrome
	-moz-border-radius radius // FF 1+
	-webkit-border-radius radius // FF 1+

// Rounded Bottom Corners
borderBottomRadius(radius)
	border-radius 0 0 radius radius // Opera 105, IE 9, Saf5, Chrome
	-moz-border-radius 0 0 radius radius // FF 1+
	-webkit-border-radius 0 0 radius radius // FF 1+

// Rounded Top Corners
borderTopRadius(radius)
	border-radius radius radius 0 0 // Opera 105, IE 9, Saf5, Chrome
	-moz-border-radius radius radius 0 0 // FF 1+
	-webkit-border-radius radius radius 0 0 // FF 1+

// Rounded Right Corners
borderRightRadius(radius)
	border-radius 0 radius radius 0 // Opera 105, IE 9, Saf5, Chrome
	-moz-border-radius 0 radius radius 0 // FF 1+
	-webkit-border-radius 0 radius radius 0 // FF 1+

// Top-to-Bottom Gradient
gradientV(start, end)
	//background (start + end) / 2 // Non CSS3 browsers get the average color
	background-image -moz-linear-gradient(top, start, end) // FF 36
	background-image -webkit-gradient(linear, left top, left bottom, from(start), to(end)) // Safari 4+, Chrome
	background-image -webkit-linear-gradient(top, start, end) // Chrome 10+, Safari 51+
	background-image -o-linear-gradient(top, start, end) // Opera 1110+
	background-image linear-gradient(top, start, end) // CSS3
	//filter %("progid DXImageTransformMicrosoftgradient(startColorstr=%d, endColorstr=%d)", start, end) // IE6 & 7
	//-ms-filter %("progid DXImageTransformMicrosoftgradient(startColorstr=%d, endColorstr=%d)", start, end) // IE8
	background-image -ms-linear-gradient(top, end, end) // IE10

// Left-to-Right Gradient
gradientH(start, end)
	//background (start + end) / 2 // Non CSS3 browsers get the average color
	background-image -moz-linear-gradient(left, start, end) // FF 36
	background-image -webkit-gradient(linear, left center, right center, from(start), to(end)) // Safari 4+, Chrome
	background-image -webkit-linear-gradient(left center, right center, from(start), to(end)) // Chrome 10+, Safari 51+
	background-image -o-linear-gradient(left, start, end) // Opera 1110+
	background-image linear-gradient(left, start, end) // CSS3
	//filter %("progid DXImageTransformMicrosoftgradient(startColorstr=%d, endColorstr=%d, GradientType=1)", start, end) // IE6 & 7
	//-ms-filter %("progid DXImageTransformMicrosoftgradient(startColorstr=%d, endColorstr=%d, GradientType=1)", start, end) // IE8
	background-image -ms-linear-gradient(left, end, end) // IE10

// Drop shadow
boxShadow(xOffset, yOffset, blur, color)
	-moz-box-shadow xOffset yOffset blur color // FF35+
	-webkit-box-shadow xOffset yOffset blur color // Saf30+, Chrome
	box-shadow xOffset yOffset blur color // Opera 105, IE9+, Chrome 10+

// Drop shadow
boxInsetShadow(xOffset, yOffset, blur, color)
	-moz-box-shadow inset xOffset yOffset blur color // FF35+
	-webkit-box-shadow inset xOffset yOffset blur color // Saf30+, Chrome
	box-shadow inset xOffset yOffset blur color // Opera 105, IE9+, Chrome 10+

textShadow(xOffset, yOffset, blur, color)
	text-shadow xOffset, yOffset, blur, color

// Transition
transition(property, time, easing)
	-moz-transition property time easing // FF4+
	-o-transition property time easing // Opera 105+
	-webkit-transition property time easing // Safari 32+, Chrome
	-ms-transition property time easing // IE10
	transition property time easing // CSS3
 
iconize() 
	position relative
	top 3px
 

